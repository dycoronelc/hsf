[build]
buildCommand = "npm install && npm run build"
watchPatterns = [
  "app/**",
  "lib/**",
  "public/**",
  "next.config.js",
  "tailwind.config.js",
  "postcss.config.js",
  "tsconfig.json",
  "package.json",
  "package-lock.json",
]

[deploy]
startCommand = "npm run start"
